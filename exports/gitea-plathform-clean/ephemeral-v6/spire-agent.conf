agent {
  data_dir = "/run/spire/agent"
  log_level = "INFO"
  server_address = "spire-server"
  server_port = "8081"
  insecure_bootstrap = true
}
plugins {
  NodeAttestor "join_token" {
    plugin_data { }
  }
  KeyManager "memory" {}
}
